<b-container fluid>
  <b-row>
    <b-col sm="12" lg="10" offset-lg="1">
      <%= simple_format @biography.information %>
      <%= link_to("link to edit", edit_page_path(1)) %>
    </b-col>
  </b-row>
</b-container>
